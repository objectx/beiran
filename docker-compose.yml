version: '2'
services:
  beirand:
    image: rlab.io/beiran
    build: .
    privileged: true
    volumes:
      - /var/run/docker.sock:/var/run/docker.sock
    networks:
     - test
    command: python -u beiran.py
networks:
  test:
